[comment {--- !doctools ---}]
[comment { support :: kettle invoke :: }]

The commands of this layer enable recipes to recursively invoke other
recipes, for the current and in other packages.

[list_begin definitions]
[call [cmd kettle] [method invoke]]
[list_end]
